Test for GitHub
好像中不行
Global setup:
 Set up git
  git config --global user.name "liunan"
  git config --global user.email liunan@msn.com
      Next steps:
  mkdir lndoc
  cd lndoc
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:liunan/lndoc.git
  git push -u origin master
      Existing Git Repo?
  cd existing_git_repo
  git remote add origin git@github.com:liunan/lndoc.git
  git push -u origin master

